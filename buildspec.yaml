version: 0.1

phases:
  install:
    runtime-versions:
        nodejs: 10
    commands:
        - echo Installing source NPM dependencies...
        - npm install
        - npm install -g @angular/cli@9.1.11
   build:
    commands:
        - echo Build started 
        #- ng build
#artifacts:
#    files:
#        - '**/*'
#    base-directory: 'dist*'
#    discard-paths: yes
